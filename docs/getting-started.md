# Getting started

Prerequisites: PHP 7.4+, Composer. The module requires wp-module-context.

```bash
composer install
composer run test
composer run lint
composer run fix
```

See [integration.md](integration.md) for using in a host plugin.
